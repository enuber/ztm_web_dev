// Arrays
// data structure
// can store functions, string, nums, booleans, undefined.
// should keep an array with similar types though inside.

var list = ['tiger', 'cat', 'bear', 'bird'];

// zero based so zero is first item...
list[0];

// array in an array would be accessed with [][] double brackets

// .shift() - removes front of array
// .pop() - removes last item of array

// .unshift() - adds to front of array
// .push() - adds to end of array

// .length() - gets length of array

// .concat(['bee', 'deer']) - add  to an array another array, creates a new array
// .sort() - sorts array
// .reverse() - reverse order of array
// .splice(index, 1) - remove an item from an array
// .splice(1, 0, "Pineapple") - add item to specific location
